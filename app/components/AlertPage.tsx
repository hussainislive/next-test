'use client';
import React from 'react'

const AlertPage = () => {
  return (
    <div>
        <button onClick={()=>{alert('Its working')}}>Add to Cart</button>
    </div>
  )
}

export default AlertPage